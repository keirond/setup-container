kibanaEnabled: false

ingress:
  enabled: true
  ingressClassName: nginx
  pathType: Prefix
  hostname: elasticsearch.167.254.190.2.nip.io

master:
  masterOnly: true
  replicaCount: 3
  persistence:
    enabled: true
    storageClass: longhorn
    size: 10Gi
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false

data:
  replicaCount: 3
  persistence:
    enabled: true
    storageClass: longhorn
    size: 50Gi
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false

coordinating:
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false

ingest:
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false

metrics:
  enabled: true